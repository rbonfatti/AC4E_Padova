# Exercise 03: Harness Pieces

Time: 01:30-02:15

## Output

Install or map one harness component for your chosen lane.

## Open

- `materials/tool_tracks.md`
- `examples/codex/README.md`
- `examples/claude/README.md`
- `examples/cursor/README.md`
- `agent-harness/`

## Task

Pick one component:

- project instructions;
- skill;
- subagent or reviewer;
- hook;
- MCP;
- goal or loop.

Copy it into your `starter_article` copy or write down the exact source and
destination paths.

## Verification

Record:

- source path;
- destination path;
- when to use it;
- what it is forbidden to access;
- one command or prompt that proves it is installed or understood.

## Lane Notes

- Codex: use `examples/codex/` and `.codex/` / `.agents/` paths.
- Claude Code: use `examples/claude/` and `.claude/` paths.
- Cursor: use `examples/cursor/` and `.cursor/` paths.
